<h2 id="title">Welcome to Ember</h2>

{{#if session.isAuthenticated}}
  <a {{ action 'invalidateSession' }}>Logout</a>
{{else}}
  {{#link-to 'signin'}}Login{{/link-to}}
{{/if}}

{{#link-to "protected" }}Protected{{/link-to}}
{{outlet}}
